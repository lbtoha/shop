meta {
  name: quizHistory
  type: http
  seq: 6
}

get {
  url: {{url}}/profile/quiz-history?type=contest
  body: multipartForm
  auth: bearer
}

params:query {
  type: contest
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
