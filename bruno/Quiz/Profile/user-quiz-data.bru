meta {
  name: user-quiz-data
  type: http
  seq: 10
}

get {
  url: {{url}}/profile/statistics
  body: text
  auth: bearer
}

headers {
  accept: application/json
  ~Content-Type: multipart/form-data
}

auth:bearer {
  token: {{token}}
}
